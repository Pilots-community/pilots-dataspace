# Copy to .env and fill in before running.
#
# The address that the OTHER machine will use to reach THIS machine.
# Must be routable from inside Docker containers — "http://localhost" will NOT work.
#
# Same network:       http://<LAN-IP>            (e.g. http://192.168.1.50)
# Tailscale:          http://<tailscale-ip>       (e.g. http://100.100.50.1)
# Cloud / public IP:  http://<public-ip>          (e.g. http://203.0.113.42)
# Single-machine test: http://172.17.0.1          (Docker bridge gateway, Linux)
#                      http://host.docker.internal (Docker Desktop, macOS/Windows)
#
# No trailing slash.
MY_PUBLIC_URL=

# API key for management endpoints
EDC_API_KEY=password
